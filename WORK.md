> это основной технический документ проекта. в нём детально расписана вся сетевая архитектура: полная топология с IP-адресами, как работает SDN внутри ЦОД (ONOS, OpenFlow, Flow-таблицы), как устроен SD-WAN оверлей (WireGuard для филиала, Tailscale для сотрудников), как настроен failover между 4G и спутником через FRR. также там находится полный docker-compose.yml, все конфигурационные файлы с комментариями и таблица бюджета с расчётом на 3 года. по сути это то, что инженер читает чтобы понять как всё устроено и воспроизвести систему с нуля
> пока что только описание файла